# Verification notes

CI should run TypeScript, unit/regression, build/smoke, and the API performance benchmark. Production verification should happen only after merge through the existing Yandex deployment chain.
