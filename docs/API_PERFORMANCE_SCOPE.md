# Scope guard

This branch intentionally does not change Yandex Cloud paid infrastructure settings. In particular it does not change prepared instances, CPU, RAM, Managed PostgreSQL tier, Odyssey pooling mode, CDN, or scaling limits.
