// AI Service is disabled based on user requirements.
// Logic replaced with algorithmic hints in hintService.ts

export const generateHint = async (secretWord: string): Promise<string> => {
  return Promise.resolve("Подсказка недоступна.");
};