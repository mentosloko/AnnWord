import React from 'react';
import { UserProfile } from '../../types';
import { ScreenContainer } from '../layout/ScreenContainer';

type Props = { userProfile: UserProfile; onOpenDictionaryStudio: () => void; onOpenAdultRoom: () => void; onOpenProfile?: () => void; };
const t = { title: 'Кабинет преподавателя', subtitle: 'Рабочий режим без игр, питомцев и монет. Здесь — ученики, прогресс и словари.', students: 'Мои ученики', dictionaries: 'Мои словари', profile: 'Профиль', code: 'Подключайте учеников по коду ребёнка.', vocab: 'Создавайте словари и назначайте их подключённым ученикам.', progress: 'Смотрите прогресс и сложные слова в разделе учеников.' };
const Card = ({ title, text, action, onClick }: { title: string; text: string; action: string; onClick: () => void }) => <button type="button" onClick={onClick} className="rounded-[1.75rem] border-2 border-cyan-50 bg-white p-5 text-left shadow-sm transition hover:-translate-y-1 hover:border-cyan-200 hover:shadow-lg"><div className="text-xl font-black text-indigo-950">{title}</div><p className="mt-2 min-h-[3.25rem] text-sm font-bold leading-relaxed text-gray-500">{text}</p><div className="mt-5 inline-flex rounded-2xl bg-cyan-600 px-4 py-2 text-sm font-black text-white">{action}</div></button>;
export const TeacherDashboardScreen: React.FC<Props> = ({ userProfile, onOpenDictionaryStudio, onOpenAdultRoom, onOpenProfile }) => <ScreenContainer className="max-w-6xl pb-20 pt-6"><section className="rounded-[2rem] bg-gradient-to-br from-cyan-700 to-indigo-700 p-6 text-white shadow-xl sm:p-8"><div className="text-xs font-black uppercase tracking-widest text-white/65">AnnWord Teacher</div><h1 className="mt-2 text-4xl font-black sm:text-5xl">{t.title}</h1><p className="mt-4 max-w-2xl text-sm font-bold leading-relaxed text-white/75 sm:text-base">{t.subtitle}</p><div className="mt-5 text-sm font-black text-white/80">{userProfile.username}</div></section><section className="mt-6 grid gap-4 md:grid-cols-3"><Card title={t.students} text={t.code} action={t.students} onClick={onOpenAdultRoom} /><Card title={t.dictionaries} text={t.vocab} action={t.dictionaries} onClick={onOpenDictionaryStudio} /><Card title={t.profile} text={t.progress} action={t.profile} onClick={onOpenProfile || onOpenAdultRoom} /></section></ScreenContainer>;
