import React, { useEffect, useMemo, useState } from 'react';
import { familyAccountService } from '../../services/familyAccountService';
import { mentorRoomService } from '../../services/mentorRoomService';
import { isPremiumActive, formatPremiumExpiresAt } from '../../services/premiumAccess';
import { getRecentFixedWords, getWordLearningSummary } from '../../services/wordLearningStats';
import { ManagedLearner, UserProfile, WordPerformance } from '../../types';
import { ScreenContainer } from '../layout/ScreenContainer';

interface Props { userProfile: UserProfile; onBackHome: () => void; onOpenDictionaryStudio: () => void; }
const attempts = (word: WordPerformance) => Math.max(0, Math.round(word.attempts || 0));
const accuracy = (word: WordPerformance) => attempts(word) ? Math.round(word.correct / attempts(word) * 100) : 0;
const learned = (word: WordPerformance) => word.correct > 0 && accuracy(word) >= 80;
const byMistakes = (a: WordPerformance, b: WordPerformance) => b.mistakes - a.mistakes || attempts(b) - attempts(a) || a.word.localeCompare(b.word);
const byFrequency = (a: WordPerformance, b: WordPerformance) => attempts(b) - attempts(a) || b.mistakes - a.mistakes || a.word.localeCompare(b.word);
const getEncounteredWords = (learner?: ManagedLearner): WordPerformance[] => Object.values(learner?.stats.wordPerformance ?? {}).filter(word => attempts(word) > 0).sort(byFrequency);
const Metric = ({ label, value }: { label: string; value: number | string }) => <div className="rounded-2xl bg-white p-4 shadow-sm"><div className="text-xs font-black text-indigo-400">{label}</div><div className="text-3xl font-black">{value}</div></div>;

export const AdultRoomScreen: React.FC<Props> = ({ userProfile, onBackHome, onOpenDictionaryStudio }) => {
  const isTeacher = userProfile.role === 'teacher' || userProfile.accountMode === 'teacher';
  const isParent = userProfile.role === 'parent' || userProfile.accountMode === 'parent';
  const premiumActive = isPremiumActive(userProfile);
  const [unlocked, setUnlocked] = useState(!isParent || isTeacher);
  const [pin, setPin] = useState('');
  const [code, setCode] = useState('');
  const [learners, setLearners] = useState<ManagedLearner[]>([]);
  const [selectedId, setSelectedId] = useState('');
  const [collectionId, setCollectionId] = useState('');
  const [busy, setBusy] = useState(false);
  const [notice, setNotice] = useState<string | null>(null);
  const learner = learners.find(item => item.id === selectedId) || learners[0];
  const collections = userProfile.dictionaryCollections || [];
  const encounteredWords = useMemo(() => getEncounteredWords(learner), [learner]);
  const learnedWords = encounteredWords.filter(learned);
  const errorWords = useMemo(() => encounteredWords.filter(word => word.mistakes > 0).sort(byMistakes), [encounteredWords]);
  const learning = useMemo(() => getWordLearningSummary(learner?.stats || { gamesPlayed: 0, gamesWon: 0, wordsGuessed: {} }), [learner]);
  const recentFixed = useMemo(() => getRecentFixedWords(learner?.stats || { gamesPlayed: 0, gamesWon: 0, wordsGuessed: {} }, 10), [learner]);
  const normalizedCode = code.trim().toUpperCase();

  const load = async () => { setBusy(true); try { const result = await mentorRoomService.loadLearners(); setLearners(result.learners); setSelectedId(result.learners[0]?.id || ''); if (!result.backendReady) setNotice('Данные кабинета пока недоступны.'); } catch (error: unknown) { setNotice(error instanceof Error ? error.message : 'Не удалось загрузить данные.'); } finally { setBusy(false); } };
  useEffect(() => { if (unlocked || isTeacher) void load(); }, [unlocked, isTeacher]);
  const unlock = async () => { if (!/^\d{4}$/.test(pin)) { setNotice('Введите PIN из 4 цифр.'); return; } setBusy(true); try { const ok = await familyAccountService.verifyParentPin(pin); if (ok) { setUnlocked(true); setNotice(null); } else setNotice('Неверный PIN.'); } catch (error: unknown) { setNotice(error instanceof Error ? error.message : 'Не удалось проверить PIN.'); } finally { setBusy(false); } };
  const connect = async () => { if (!normalizedCode) { setNotice('Введите код ребёнка.'); return; } setBusy(true); try { await mentorRoomService.connectByChildCode(normalizedCode); setCode(''); setNotice('Ученик подключён.'); await load(); } catch (error: unknown) { setNotice(error instanceof Error ? error.message : 'Не удалось подключить ученика.'); } finally { setBusy(false); } };
  const assign = async () => { if (!learner || !collectionId) return; setBusy(true); try { await mentorRoomService.assignCollection(learner.id, collectionId); setNotice('Словарь назначен ученику.'); await load(); } catch (error: unknown) { setNotice(error instanceof Error ? error.message : 'Не удалось назначить словарь.'); } finally { setBusy(false); } };
  const copyChildCode = async (childCode: string) => { if (!premiumActive) { setNotice('Код преподавателя доступен в Kids Premium.'); return; } try { await navigator.clipboard.writeText(childCode); setNotice('Код скопирован. Передайте его преподавателю.'); } catch { setNotice('Не удалось скопировать код. Скопируйте его вручную.'); } };

  if (isParent && !isTeacher && !unlocked) return <ScreenContainer className="max-w-md pb-20"><button type="button" onClick={onBackHome} className="mb-5 rounded-xl border-2 border-indigo-100 px-4 py-2 font-bold text-indigo-700">← Назад</button><section className="rounded-[2rem] bg-white p-6 shadow-sm"><h1 className="text-2xl font-black text-indigo-950">Кабинет родителя</h1><p className="mt-2 text-sm font-bold text-gray-500">Введите PIN, созданный при добавлении ребёнка на этом устройстве.</p><div className="mt-4 rounded-2xl bg-indigo-50 p-3 text-xs font-bold text-indigo-700">PIN защищает кабинет взрослого от случайного входа ребёнка.</div>{notice && <p role="alert" className="mt-4 rounded-xl bg-rose-50 p-3 text-sm font-bold text-rose-700">{notice}</p>}<input value={pin} onChange={event => { setPin(event.target.value.replace(/\D/g, '').slice(0, 4)); if (notice) setNotice(null); }} inputMode="numeric" maxLength={4} placeholder="••••" className="mt-5 w-full rounded-xl border-2 border-indigo-100 p-3 text-center text-xl font-black" /><p className="mt-2 text-center text-xs font-bold text-gray-400">4 цифры. Только для этого устройства.</p><button type="button" disabled={busy} onClick={() => void unlock()} className="mt-4 w-full rounded-xl bg-indigo-600 p-3 font-black text-white">Открыть</button></section></ScreenContainer>;

  return <ScreenContainer className="max-w-6xl pb-20"><header className="mb-5 flex items-center justify-between gap-3"><button type="button" onClick={onBackHome} className="rounded-xl border-2 border-indigo-100 px-4 py-2 font-bold text-indigo-700">←</button><div className="text-center"><div className="text-xs font-black uppercase tracking-widest text-indigo-400">{isTeacher ? 'AnnWord Teacher' : 'AnnWord Kids'}</div><h1 className="text-xl font-black text-indigo-950 sm:text-3xl">{isTeacher ? 'Кабинет преподавателя' : 'Кабинет родителя'}</h1></div>{isTeacher || premiumActive ? <button type="button" onClick={onOpenDictionaryStudio} className="rounded-xl bg-indigo-600 px-4 py-2 font-black text-white">Словари</button> : <button type="button" onClick={onOpenDictionaryStudio} className="rounded-xl bg-purple-50 px-3 py-2 text-xs font-black text-purple-700">Словари · Premium</button>}</header>
    {!isTeacher && <section className={`mb-5 rounded-3xl border-2 p-4 ${premiumActive ? 'border-green-100 bg-green-50' : 'border-amber-100 bg-amber-50'}`}><div className={`text-xs font-black uppercase tracking-widest ${premiumActive ? 'text-green-600' : 'text-amber-600'}`}>{premiumActive ? 'Kids Premium активен' : 'Бесплатный Kids'}</div><p className="mt-1 text-sm font-bold text-gray-600">{premiumActive ? `Доступ открыт до: ${formatPremiumExpiresAt(userProfile.premiumExpiresAt)}.` : 'Игры, питомец, магазин и общий детский словарь доступны бесплатно. Код преподавателя, отчёты и расширенные словари открываются в Premium.'}</p></section>}
    {notice && <p role="status" aria-live="polite" className="mb-4 rounded-xl bg-indigo-50 p-3 text-sm font-bold text-indigo-700">{notice}</p>}
    {isTeacher && <section className="mb-5 rounded-3xl bg-white p-5 shadow-sm"><h2 className="font-black text-indigo-950">Подключить ученика по коду</h2><p className="mt-1 text-sm font-bold text-gray-500">Введите код, который родитель передал из своего кабинета.</p><div className="mt-3 flex flex-col gap-2 sm:flex-row"><input value={code} onChange={event => setCode(event.target.value.toUpperCase())} placeholder="Код ребёнка" className="min-w-0 flex-1 rounded-xl border-2 border-indigo-100 px-3 py-2 font-black" /><button type="button" disabled={busy || !normalizedCode} onClick={() => void connect()} className="rounded-xl bg-indigo-600 px-4 py-3 font-black text-white disabled:bg-gray-200 disabled:text-gray-400">Подключить</button></div></section>}
    <div className="grid gap-5 lg:grid-cols-[280px_1fr]"><aside className="rounded-3xl bg-white p-4 shadow-sm"><h2 className="mb-3 text-xs font-black uppercase text-indigo-400">{isTeacher ? 'Ученики' : 'Ребёнок'}</h2>{busy && !learners.length ? <p>Загружаю...</p> : learners.length ? learners.map(item => <button key={item.id} type="button" onClick={() => setSelectedId(item.id)} aria-pressed={selectedId === item.id} className={`mb-2 w-full rounded-xl p-3 text-left font-black ${selectedId === item.id ? 'bg-indigo-600 text-white' : 'bg-indigo-50 text-indigo-950'}`}>{item.name}</button>) : <div className="rounded-xl bg-indigo-50 p-3 text-sm font-bold text-indigo-700">{isTeacher ? 'Пока нет подключённых учеников.' : 'Профиль ребёнка не найден.'}</div>}{!isTeacher && learner?.childShareCode && (premiumActive ? <div className="mt-4 rounded-xl bg-purple-50 p-3"><div className="text-xs font-black text-purple-500">КОД ДЛЯ ПРЕПОДАВАТЕЛЯ</div><div className="mt-2 text-center text-xl font-black tracking-widest text-purple-800">{learner.childShareCode}</div><p className="mt-2 text-xs font-bold text-purple-700">Передайте этот код учителю.</p><button type="button" onClick={() => void copyChildCode(learner.childShareCode || '')} className="mt-3 w-full rounded-xl bg-purple-600 px-3 py-2 text-sm font-black text-white">Скопировать код</button></div> : <div className="mt-4 rounded-xl bg-amber-50 p-3"><div className="text-xs font-black text-amber-600">Код преподавателя</div><p className="mt-2 text-sm font-bold text-amber-800">Доступен в Kids Premium.</p></div>)}{!isTeacher && <div className={`mt-4 rounded-xl p-3 ${premiumActive ? 'bg-indigo-50' : 'bg-gray-50'}`}><div className={`text-xs font-black uppercase ${premiumActive ? 'text-indigo-500' : 'text-gray-400'}`}>Отчёт на почту</div><p className="mt-2 text-sm font-bold text-gray-500">{premiumActive ? (userProfile.weeklyReportEmail || 'Email можно добавить в настройках отчётов.') : 'Еженедельные отчёты доступны в Premium.'}</p></div>}</aside>
      {learner ? <main className="space-y-4"><div className="grid grid-cols-2 gap-3 md:grid-cols-4"><Metric label="ИГР" value={learner.stats.gamesPlayed} /><Metric label="ПОБЕД" value={learner.stats.gamesWon} /><Metric label="СЛОВ ВСТРЕТИЛОСЬ" value={encounteredWords.length} /><Metric label="ОШИБОЧНЫХ" value={errorWords.length} /></div><div className="grid grid-cols-2 gap-3 md:grid-cols-4"><Metric label="ВЫУЧЕНО" value={learnedWords.length} /><Metric label="АКТИВНЫЙ СЛОВАРЬ" value={learner.assignedWords.length || userProfile.customDictionaryEn.length} /><Metric label="К ПОВТОРЕНИЮ" value={learning.activeReview.length} /><Metric label="ИСПРАВЛЕНО" value={learning.fixedAfterMistake.length} /></div>{isTeacher && <section className="rounded-3xl bg-white p-5 shadow-sm"><h2 className="font-black">Назначить сохранённый словарь</h2><p className="mt-1 text-sm font-bold text-gray-500">Словарь появится у ребёнка, если у семьи активен Premium.</p><div className="mt-3 flex gap-2"><select value={collectionId} onChange={event => setCollectionId(event.target.value)} className="min-w-0 flex-1 rounded-xl border-2 border-indigo-100 p-2 font-bold"><option value="">Выберите подборку</option>{collections.map(item => <option key={item.id} value={item.id}>{item.title}</option>)}</select><button type="button" disabled={!collectionId || busy} onClick={() => void assign()} className="rounded-xl bg-indigo-600 px-4 font-black text-white disabled:bg-gray-200 disabled:text-gray-400">Назначить</button></div></section>}<section className="rounded-3xl bg-white p-5 shadow-sm"><h2 className="font-black">Ошибки и исправления</h2><p className="mt-1 text-sm font-bold text-gray-500">Эти события сохраняются с датами и смогут попасть в отчёт за выбранный период.</p><div className="mt-4 grid gap-3 sm:grid-cols-3"><Metric label="ОШИБОК ВСЕГО" value={learning.totalMistakes} /><Metric label="ИСПРАВЛЕНИЙ" value={learning.totalResolutions} /><Metric label="СЛОЖНЫХ СЛОВ" value={learning.totalDifficultWords} /></div><div className="mt-4 rounded-2xl bg-green-50 p-4"><div className="text-sm font-black text-green-800">Последние исправленные слова</div><div className="mt-3 flex flex-wrap gap-2">{recentFixed.length ? recentFixed.map(item => <span key={item.word} className="rounded-full bg-white px-3 py-1 text-xs font-black text-green-700 shadow-sm">{item.word} · исправлено: {item.resolvedCount}</span>) : <span className="text-sm font-bold text-green-700/70">Пока нет исправленных после ошибки слов.</span>}</div></div></section><section className="rounded-3xl bg-white p-5 shadow-sm"><h2 className="font-black">Ошибочные слова</h2><div className="mt-3 flex flex-wrap gap-2">{errorWords.length ? errorWords.map(word => <span key={word.word} className="rounded-full bg-rose-50 px-3 py-1 text-xs font-black text-rose-700">{word.word} · ошибок: {word.mistakes} · {attempts(word)}×</span>) : <span className="text-sm text-gray-500">Ошибочных слов пока нет.</span>}</div></section><section className="rounded-3xl bg-white p-5 shadow-sm"><h2 className="font-black">Слова в тренировках</h2><div className="mt-3 flex flex-wrap gap-2">{encounteredWords.length ? encounteredWords.map(word => <span key={word.word} className="rounded-full bg-indigo-50 px-3 py-1 text-xs font-black text-indigo-700">{word.word} · {attempts(word)}× · ошибок: {word.mistakes}</span>) : <span className="text-sm text-gray-500">Появятся после завершённых тренировок.</span>}</div></section></main> : <main className="rounded-3xl bg-white p-5 text-sm font-bold text-gray-500 shadow-sm">Выберите ребёнка или подключите ученика.</main>}
    </div>
  </ScreenContainer>;
};