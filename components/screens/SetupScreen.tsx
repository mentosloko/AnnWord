import React from 'react';
import { DictionarySource, DifficultyLevel, GameSettings, UserProfile, WordLength } from '../../types';
import { getPremiumDictionaryCatalog, getPremiumDictionaryMeta, getPremiumDictionaryWords, hasPremiumDictionaryAccess } from '../../services/premiumDictionaryCatalog';
import { ScreenContainer } from '../layout/ScreenContainer';
import { PlayableModeRoute } from '../AppScreens';

interface SetupScreenProps { selectedPlayMode: PlayableModeRoute; settings: GameSettings; customDictionaryWords: string[]; setupError: string | null; isUploadingDictionary: boolean; isAuthenticated: boolean; userProfile: UserProfile; onSettingsChange: (settings: GameSettings) => void; onFileUpload: (event: React.ChangeEvent<HTMLInputElement>) => void; onOpenDictionaryStudio: () => void; onOpenPremium: () => void; onStartGame: () => void; onBack: () => void; onLogin: () => void; }
const WORD_LENGTHS: WordLength[] = [4, 5, 6];
const DIFFICULTIES: Array<{ value: DifficultyLevel; short: string }> = [{ value: 'ALL', short: 'Все' }, { value: 'A1', short: 'A1' }, { value: 'A2', short: 'A2' }, { value: 'B1', short: 'B1' }, { value: 'B2', short: 'B2' }, { value: 'C1', short: 'C1' }, { value: 'C2', short: 'C2' }];
const MODE_LABELS: Record<PlayableModeRoute, string> = { game: 'Классика', anagrams: 'Анаграммы', translation: '1 из 2', sprint: 'Спринт', memory: 'Память', hangman: 'Виселица' };
const LENGTH_AGNOSTIC_MODES = new Set<PlayableModeRoute>(['translation', 'sprint', 'memory']);
export const SetupScreen: React.FC<SetupScreenProps> = ({ selectedPlayMode, settings, customDictionaryWords, setupError, isUploadingDictionary, isAuthenticated, userProfile, onOpenDictionaryStudio, onOpenPremium, onSettingsChange, onStartGame, onBack, onLogin }) => {
  const isCustomDictionary = settings.dictionarySource === 'custom'; const isPremiumDictionary = settings.dictionarySource === 'premium'; const hasPremium = hasPremiumDictionaryAccess(userProfile); const premiumCatalog = getPremiumDictionaryCatalog(); const activePremium = getPremiumDictionaryMeta(settings.activePremiumDictionaryId); const ignoresWordLength = LENGTH_AGNOSTIC_MODES.has(selectedPlayMode); const matchesModeLength = (word: string) => ignoresWordLength || word.trim().length === settings.wordLength; const customWordsCount = customDictionaryWords.filter(matchesModeLength).length; const premiumWordsCount = getPremiumDictionaryWords(settings.activePremiumDictionaryId, settings.difficulty).filter(matchesModeLength).length; const canStart = isCustomDictionary ? hasPremium && customWordsCount > 0 : isPremiumDictionary ? hasPremium && premiumWordsCount > 0 : true;
  const updateSetting = <K extends keyof GameSettings>(key: K, value: GameSettings[K]) => onSettingsChange({ ...settings, [key]: value });
  const selectDictionarySource = (source: DictionarySource) => { if ((source === 'custom' || source === 'premium') && !isAuthenticated) { onLogin(); return; } if ((source === 'custom' || source === 'premium') && !hasPremium) { onOpenPremium(); return; } onSettingsChange({ ...settings, dictionarySource: source, useCustomDictionary: source === 'custom' }); };
  const selectPremiumDictionary = (id: string) => onSettingsChange({ ...settings, dictionarySource: 'premium', useCustomDictionary: false, activePremiumDictionaryId: id });
  const showDifficulty = settings.dictionarySource === 'builtin' || (isPremiumDictionary && hasPremium);
  const emptyWordsText = ignoresWordLength ? 'Нет слов для этого режима' : `Нет слов из ${settings.wordLength} букв`;
  return <ScreenContainer className="max-w-3xl px-3 pb-20 pt-3 sm:px-4"><div className="mb-3 flex items-center justify-between gap-3"><button type="button" onClick={onBack} aria-label="Назад" className="flex h-11 w-11 items-center justify-center rounded-2xl border-2 border-indigo-100 bg-white text-2xl font-black text-indigo-700">←</button><div className="text-center"><div className="text-xs font-black uppercase tracking-widest text-indigo-400">{MODE_LABELS[selectedPlayMode]}</div><h1 className="text-2xl font-black text-indigo-950 sm:text-3xl">Перед стартом</h1></div><div className="h-11 w-11" /></div><div className="rounded-[2rem] border-2 border-indigo-50 bg-white p-4 shadow-sm sm:p-6">{setupError && <div role="alert" aria-live="assertive" className="mb-4 rounded-2xl border-2 border-red-100 bg-red-50 px-4 py-3 text-sm font-bold text-red-700">{setupError}</div>}{!ignoresWordLength && <section aria-labelledby="word-length-title"><h2 id="word-length-title" className="mb-2 text-xs font-black uppercase tracking-widest text-indigo-400">Длина слова</h2><div className="grid grid-cols-3 gap-2" role="group" aria-label="Длина слова">{WORD_LENGTHS.map(length => <button type="button" key={length} aria-pressed={settings.wordLength === length} onClick={() => updateSetting('wordLength', length)} className={`rounded-2xl py-3 font-black ${settings.wordLength === length ? 'bg-indigo-600 text-white' : 'border-2 border-indigo-100 text-indigo-700'}`}>{length}</button>)}</div></section>} {ignoresWordLength && <section aria-labelledby="word-length-title"><h2 id="word-length-title" className="mb-2 text-xs font-black uppercase tracking-widest text-indigo-400">Длина слова</h2><div className="rounded-2xl bg-indigo-50 px-4 py-3 text-sm font-bold text-indigo-700">В этом режиме используются слова любой длины.</div></section>}<section className="mt-4" aria-labelledby="dictionary-source-title"><h2 id="dictionary-source-title" className="mb-2 text-xs font-black uppercase tracking-widest text-indigo-400">Слова для игры</h2><div className="grid grid-cols-3 gap-2" role="group" aria-label="Источник слов"><button type="button" aria-pressed={settings.dictionarySource === 'builtin'} onClick={() => selectDictionarySource('builtin')} className={`rounded-2xl border-2 p-3 text-left ${settings.dictionarySource === 'builtin' ? 'border-indigo-300 bg-indigo-50' : 'border-indigo-100'}`}><div className="text-xl" aria-hidden="true">📚</div><div className="text-sm font-black">Встроенный</div><div className="text-[11px] font-bold text-gray-400">по уровням</div></button><button type="button" aria-pressed={isCustomDictionary && hasPremium} aria-label={hasPremium ? `Мой словарь, ${customWordsCount} слов` : 'Мой словарь, нужен Premium'} onClick={() => selectDictionarySource('custom')} className={`relative rounded-2xl border-2 p-3 text-left ${isCustomDictionary && hasPremium ? 'border-purple-300 bg-purple-50' : 'border-indigo-100'}`}><span className="absolute right-3 top-3 text-sm" aria-hidden="true">{hasPremium ? '✨' : '🔒'}</span><div className="text-xl" aria-hidden="true">🧩</div><div className="text-sm font-black">Мой словарь</div><div className="text-[11px] font-bold text-gray-400">{hasPremium ? `${customWordsCount} слов` : 'Premium'}</div></button><button type="button" aria-pressed={isPremiumDictionary && hasPremium} aria-label={hasPremium ? `Premium словарь: ${activePremium.shortTitle}` : 'Premium словари, нужен Premium'} onClick={() => selectDictionarySource('premium')} className={`relative rounded-2xl border-2 p-3 text-left ${isPremiumDictionary && hasPremium ? 'border-amber-300 bg-amber-50' : 'border-indigo-100'}`}><span className="absolute right-3 top-3 text-sm" aria-hidden="true">{hasPremium ? '✅' : '🔒'}</span><div className="text-xl" aria-hidden="true">{activePremium.icon}</div><div className="text-sm font-black">Premium</div><div className="text-[11px] font-bold text-gray-400">{hasPremium ? `${activePremium.shortTitle} · ${premiumWordsCount}` : '10 тем'}</div></button></div>{!hasPremium && isAuthenticated && <p className="mt-2 rounded-2xl bg-purple-50 px-4 py-3 text-xs font-bold text-purple-700">Мой словарь и Premium-подборки открываются после подключения Premium. Нажмите на закрытую карточку, чтобы посмотреть условия.</p>}</section>{showDifficulty && <section className="mt-4" aria-labelledby="difficulty-title"><h2 id="difficulty-title" className="mb-2 text-xs font-black uppercase tracking-widest text-indigo-400">{isPremiumDictionary ? 'Уровень Premium-словаря' : 'Сложность'}</h2><div className="grid grid-cols-4 gap-2 sm:grid-cols-7" role="group" aria-label="Сложность словаря">{DIFFICULTIES.map(level => { const count = isPremiumDictionary ? getPremiumDictionaryWords(settings.activePremiumDictionaryId, level.value).filter(matchesModeLength).length : null; const disabled = isPremiumDictionary && count === 0; return <button type="button" key={level.value} aria-pressed={settings.difficulty === level.value} disabled={disabled} onClick={() => updateSetting('difficulty', level.value)} className={`rounded-2xl py-2.5 text-sm font-black disabled:opacity-35 ${settings.difficulty === level.value ? 'bg-indigo-600 text-white' : 'border-2 border-indigo-100 text-indigo-700'}`}><span>{level.short}</span>{isPremiumDictionary && <span className="mt-0.5 block text-[10px] font-bold opacity-75">{count}</span>}</button>; })}</div>{isPremiumDictionary && <p className="mt-2 rounded-2xl bg-amber-50 px-4 py-3 text-xs font-bold text-amber-700">Уровень применяется только к выбранному Premium-словарю{ignoresWordLength ? '.' : ' и текущей длине слова.'}</p>}</section>}{isPremiumDictionary && hasPremium && <section className="mt-4 rounded-2xl border-2 border-dashed border-amber-100 bg-amber-50/50 p-4"><h2 className="mb-3 text-xs font-black uppercase tracking-widest text-amber-500">Premium-словарь</h2><div className="grid grid-cols-2 gap-2 sm:grid-cols-5" role="group" aria-label="Выбор Premium-словаря">{premiumCatalog.map(item => <button type="button" key={item.id} aria-pressed={settings.activePremiumDictionaryId === item.id} onClick={() => selectPremiumDictionary(item.id)} className={`rounded-2xl border-2 p-3 text-left transition ${settings.activePremiumDictionaryId === item.id ? 'border-amber-400 bg-white shadow-sm' : 'border-white bg-white/70 hover:bg-white'}`}><div className="text-xl" aria-hidden="true">{item.icon}</div><div className="truncate text-xs font-black text-indigo-950">{item.shortTitle}</div><div className="text-[10px] font-black text-amber-700">{item.wordCount} слов</div></button>)}</div></section>}{isCustomDictionary && hasPremium && <section className="mt-4 rounded-2xl border-2 border-dashed border-purple-100 bg-purple-50/50 p-4"><div className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><span className="block font-black text-indigo-950">{customDictionaryWords.length ? 'Словарь готов к игре' : 'Создайте словарь'}</span><span className="block text-xs font-bold text-gray-500">Загрузка и распознавание — в студии словарей</span></div><button type="button" onClick={onOpenDictionaryStudio} className="rounded-xl bg-purple-600 px-4 py-2.5 text-sm font-black text-white">Открыть студию</button></div>{isUploadingDictionary && <p className="mt-2 text-xs font-bold text-purple-700">Сохраняю словарь...</p>}</section>}<button type="button" onClick={onStartGame} disabled={!canStart} className={`mt-5 w-full rounded-2xl py-4 font-black ${canStart ? 'bg-indigo-600 text-white' : 'bg-gray-100 text-gray-400'}`}>{canStart ? `Начать: ${MODE_LABELS[selectedPlayMode]}` : isPremiumDictionary && !hasPremium ? 'Нужен Premium' : isCustomDictionary && !hasPremium ? 'Нужен Premium' : emptyWordsText}</button></div></ScreenContainer>;
};
