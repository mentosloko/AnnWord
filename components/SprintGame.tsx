import React, { useState, useEffect, useCallback, useMemo, useRef } from 'react';
import { EnrichedWord, UserProfile } from '../types';
import { COMMON_WORDS_EN } from '../dictionaries/english';
import { hasRussianTranslation, normalizeWord } from '../services/dictionaryEngine';
import { buildPlayableGameDictionary, pickAdaptiveSessionWord, updateReviewPriorities, WordPracticeResult } from '../services/gameSessionEngine';
import { motion } from 'motion/react';
import { GameResultOverlay } from './GameResultOverlay';
import { applyGameRewardToCharacter, calculateGameReward, CharacterProgressResult, GameRewardInput } from '../services/gamificationRules';
import { CoinIcon } from './CoinIcon';
import { isKidsMode } from '../services/modeFlags';

interface SprintGameProps { onBack: () => void; userProfile: UserProfile; onGameReward: (input: GameRewardInput) => void | Promise<void>; onWordPractice?: (word: string, result: WordPracticeResult) => void | Promise<void>; paused?: boolean; }
interface SprintQuestion { id: number; word: EnrichedWord; options: string[]; correctAnswer: string; }
export const buildSprintDictionary = (customDictionaryEn: string[] = [], fallbackDictionary: EnrichedWord[] = COMMON_WORDS_EN): EnrichedWord[] => buildPlayableGameDictionary(customDictionaryEn, fallbackDictionary);
const hasEnoughSprintOptions = (entries: EnrichedWord[]): boolean => new Set(entries.map(entry => entry.translation).filter(hasRussianTranslation)).size >= 4;
const getCoinProgress = (score: number) => score >= 10 ? { label: '10+ звёзд: большой бонус 3 монеты', progress: 100 } : score >= 6 ? { label: `6+ звёзд: 1 монета. Ещё ${10 - score} до 3 монет`, progress: Math.round(score / 10 * 100) } : { label: `Звёзды = правильные ответы. Ещё ${6 - score} до 1 монеты`, progress: Math.round(score / 6 * 60) };
export const pickSprintRoundWord = (entries: EnrichedWord[], reviewPriorities: Record<string, number>, usedWords: Set<string>, previousWord?: string, random: () => number = Math.random): EnrichedWord | null => { if (!entries.length) return null; let candidates = entries.filter(entry => !usedWords.has(normalizeWord(entry.word))); if (!candidates.length) { usedWords.clear(); candidates = entries; } const selected = pickAdaptiveSessionWord('sprint', candidates, reviewPriorities, previousWord, random) || candidates[Math.floor(random() * candidates.length)] || null; if (selected) usedWords.add(normalizeWord(selected.word)); return selected; };

export const SprintGame: React.FC<SprintGameProps> = ({ onBack, userProfile, onGameReward, onWordPractice, paused = false }) => {
  const dictionary = useMemo(() => buildSprintDictionary(userProfile.customDictionaryEn), [userProfile.customDictionaryEn]);
  const activeDictionaryRef = useRef<EnrichedWord[]>(dictionary), latestDictionaryRef = useRef<EnrichedWord[]>(dictionary), usedWordsRef = useRef<Set<string>>(new Set());
  const nextWordTimeoutRef = useRef<ReturnType<typeof setTimeout> | null>(null), rewardAppliedRef = useRef(false), questionIdRef = useRef(0);
  const [reviewPriorities, setReviewPriorities] = useState<Record<string, number>>({ ...(userProfile.stats.wordsToReview || {}) });
  const [question, setQuestion] = useState<SprintQuestion | null>(null), [score, setScore] = useState(0), [timeLeft, setTimeLeft] = useState(60);
  const [status, setStatus] = useState<'playing' | 'ended'>('playing'), [feedback, setFeedback] = useState<'correct' | 'wrong' | null>(null);
  const [resultProgress, setResultProgress] = useState<CharacterProgressResult | null>(null);
  const showKidsRewards = isKidsMode(userProfile), dictionaryIsPlayable = hasEnoughSprintOptions(dictionary), coinProgress = getCoinProgress(score);
  useEffect(() => { latestDictionaryRef.current = dictionary; if (activeDictionaryRef.current.length === 0 && dictionary.length > 0) activeDictionaryRef.current = dictionary; }, [dictionary]);
  useEffect(() => setReviewPriorities({ ...(userProfile.stats.wordsToReview || {}) }), [userProfile.stats.wordsToReview]);
  const clearNextWordTimeout = useCallback(() => { if (nextWordTimeoutRef.current) { clearTimeout(nextWordTimeoutRef.current); nextWordTimeoutRef.current = null; } }, []);
  const registerPractice = (word: string, result: WordPracticeResult) => { setReviewPriorities(previous => updateReviewPriorities(previous, word, result)); void Promise.resolve(onWordPractice?.(word, result)).catch(error => console.error('Failed to save sprint practice priority', error)); };
  const buildQuestion = useCallback((previousWord?: string): SprintQuestion | null => { const activeDictionary = activeDictionaryRef.current; if (!hasEnoughSprintOptions(activeDictionary)) return null; const word = pickSprintRoundWord(activeDictionary, reviewPriorities, usedWordsRef.current, previousWord); if (!word) return null; const correctAnswer = word.translation; const wrongOptions: string[] = Array.from(new Set<string>(activeDictionary.map(entry => entry.translation).filter((candidate): candidate is string => hasRussianTranslation(candidate) && candidate !== correctAnswer))).sort(() => Math.random() - 0.5).slice(0, 3); questionIdRef.current += 1; return { id: questionIdRef.current, word, correctAnswer, options: [...wrongOptions, correctAnswer].sort(() => Math.random() - 0.5) }; }, [reviewPriorities]);
  const pickNewWord = useCallback(() => { setQuestion(previous => buildQuestion(previous?.word.word) || previous); setFeedback(null); }, [buildQuestion]);
  const restartGame = () => { clearNextWordTimeout(); activeDictionaryRef.current = latestDictionaryRef.current; usedWordsRef.current.clear(); rewardAppliedRef.current = false; questionIdRef.current = 0; setQuestion(null); setScore(0); setTimeLeft(60); setFeedback(null); setResultProgress(null); setStatus('playing'); };
  useEffect(() => { if (dictionaryIsPlayable && status === 'playing' && !question) pickNewWord(); }, [dictionaryIsPlayable, status, question, pickNewWord]);
  useEffect(() => { if (!dictionaryIsPlayable || status !== 'playing' || paused) return; const timer = setInterval(() => { setTimeLeft(previous => { if (previous <= 1) { clearInterval(timer); clearNextWordTimeout(); setStatus('ended'); return 0; } return previous - 1; }); }, 1000); return () => { clearInterval(timer); clearNextWordTimeout(); }; }, [dictionaryIsPlayable, status, paused, clearNextWordTimeout]);
  useEffect(() => { if (status !== 'ended' || rewardAppliedRef.current) return; rewardAppliedRef.current = true; const reward = calculateGameReward({ type: 'sprint', guessedWords: score }); setResultProgress(showKidsRewards ? applyGameRewardToCharacter(userProfile.pet, reward) : null); void Promise.resolve(onGameReward({ type: 'sprint', guessedWords: score })).catch(error => console.error('Failed to save Sprint result', error)); }, [status, score, onGameReward, showKidsRewards, userProfile.pet]);
  const rewardPreview = calculateGameReward({ type: 'sprint', guessedWords: score });
  const handleOptionClick = (option: string) => { if (status !== 'playing' || feedback || !question || paused) return; clearNextWordTimeout(); if (option === question.correctAnswer) { setScore(previous => previous + 1); setFeedback('correct'); registerPractice(question.word.word, 'mastered'); nextWordTimeoutRef.current = setTimeout(pickNewWord, 500); } else { setFeedback('wrong'); registerPractice(question.word.word, 'failed'); nextWordTimeoutRef.current = setTimeout(pickNewWord, 800); } };
  if (dictionary.length === 0) return <div className="flex w-full max-w-md flex-col items-center justify-center rounded-3xl bg-white p-8 text-center shadow-2xl"><div className="mb-4 text-6xl">📚</div><h2 className="mb-2 text-2xl font-bold">Нет доступных слов</h2><p className="mb-6 text-gray-500">В выбранном словаре нет слов с русским переводом.</p><button onClick={onBack} className="rounded-lg bg-indigo-600 px-6 py-2 font-bold text-white">Назад</button></div>;
  if (!dictionaryIsPlayable) return <div className="flex w-full max-w-md flex-col items-center justify-center rounded-3xl bg-white p-8 text-center shadow-2xl"><div className="mb-4 text-6xl">⚡</div><h2 className="mb-2 text-2xl font-bold">Недостаточно слов для Спринта</h2><p className="mb-6 text-gray-500">Для игры нужно не менее 4 слов с разными переводами.</p><button onClick={onBack} className="rounded-lg bg-indigo-600 px-6 py-2 font-bold text-white">Назад</button></div>;
  if (status === 'ended') return <GameResultOverlay isOpen status="completed" title="Спринт завершён" subtitle={showKidsRewards ? `Звёзд: ${score}. ${rewardPreview.coins > 0 ? `Монеты: +${rewardPreview.coins}.` : 'До монет нужно минимум 6 звёзд.'}` : `Звёзд: ${score}. Слова добавлены в статистику практики.`} emoji="🏆" pet={resultProgress?.pet} xpGained={showKidsRewards ? rewardPreview.xp : 0} coinsGained={showKidsRewards ? rewardPreview.coins : 0} onPrimary={restartGame} onSecondary={onBack} />;
  return <div className="relative flex h-full min-h-0 w-full max-w-md flex-col items-center overflow-hidden rounded-3xl bg-white p-3 shadow-xl sm:h-auto sm:p-6" aria-busy={paused}><div className="absolute left-0 top-0 h-2 w-full bg-indigo-100"><motion.div className="h-full bg-indigo-600" initial={{ width: '100%' }} animate={{ width: `${(timeLeft / 60) * 100}%` }} transition={{ duration: paused ? 0 : 1, ease: 'linear' }} /></div><div className="mb-2 mt-2 flex w-full shrink-0 justify-end gap-2 sm:mb-4 sm:mt-4"><div className="flex items-center gap-1.5"><div className="flex min-h-[2.4rem] items-center gap-1 rounded-full bg-gray-100 px-3 py-1"><span aria-hidden="true">⏱️</span><span className={`font-mono text-base font-bold ${timeLeft < 10 ? 'animate-pulse text-red-500' : 'text-gray-700'}`}>{timeLeft}с</span></div><div className="flex min-h-[2.4rem] items-center gap-1 rounded-full bg-indigo-50 px-3 py-1"><span aria-hidden="true">⭐</span><span className="text-base font-bold text-indigo-600">{score}</span></div></div></div>{showKidsRewards && <div className="mb-3 w-full rounded-2xl bg-amber-50 px-3 py-2 text-xs font-bold text-amber-800"><div className="flex items-center justify-between gap-2"><span>{coinProgress.label}</span><span className="inline-flex items-center gap-1">{rewardPreview.coins}<CoinIcon /></span></div><div className="mt-2 h-2 overflow-hidden rounded-full bg-amber-100"><div className="h-full rounded-full bg-amber-500" style={{ width: `${coinProgress.progress}%` }} /></div></div>}{paused && <div className="mb-2 rounded-full bg-indigo-50 px-3 py-1 text-xs font-bold uppercase tracking-wider text-indigo-500">Пауза</div>}<motion.div key={question?.id || 'empty'} initial={{ x: 50, opacity: 0 }} animate={{ x: 0, opacity: 1 }} className="mb-4 mt-1 shrink-0 text-center sm:mb-10"><div className="mb-1 text-xs font-bold uppercase tracking-wider text-gray-400 sm:text-sm">Как переводится?</div><div className="break-words text-[clamp(1.9rem,9vw,2.65rem)] font-bold tracking-tight text-indigo-900 sm:text-5xl">{question?.word.word}</div></motion.div><div className="grid min-h-0 w-full flex-1 grid-cols-1 content-center gap-2 sm:flex-none sm:gap-3">{question?.options.map((option, index) => <motion.button key={`${question.id}-${option}-${index}`} aria-label={`Ответ: ${option}`} whileHover={{ scale: paused ? 1 : 1.02 }} whileTap={{ scale: paused ? 1 : 0.98 }} disabled={paused || status !== 'playing'} onClick={() => handleOptionClick(option)} className={`flex min-h-[2.8rem] w-full items-center justify-between rounded-2xl border-2 px-4 py-[clamp(0.65rem,1.85dvh,1.05rem)] text-sm font-bold transition-all disabled:opacity-60 sm:px-6 sm:text-lg ${feedback === 'correct' && option === question.correctAnswer ? 'border-green-600 bg-green-500 text-white shadow-lg' : feedback === 'wrong' && option !== question.correctAnswer ? 'border-gray-100 bg-white text-gray-400 opacity-50' : feedback === 'wrong' && option === question.correctAnswer ? 'border-green-500 bg-green-100 text-green-700' : 'border-gray-100 bg-white text-gray-700 shadow-sm hover:border-indigo-200 hover:bg-indigo-50'}`}><span>{option}</span>{feedback === 'correct' && option === question.correctAnswer && <span aria-hidden="true">✅</span>}</motion.button>)}</div></div>;
};
